# SeeClick

## 一、基本信息

- **论文标题**：SeeClick: Harnessing GUI Grounding for Advanced Visual GUI Agents
- **作者**：Kanzhi Cheng 等
- **发表会议**：ACL 2024
- **研究方向**：多模态大模型、GUI Agent、视觉界面理解、智能交互
- **论文代码**：[njucckevin/SeeClick](https://github.com/njucckevin/SeeClick)

## 二、一句话介绍

SeeClick 是一个只依赖屏幕截图进行操作的视觉 GUI Agent，论文的核心观点是：**GUI Agent 能否准确定位界面元素，是影响其任务执行效果的关键因素。**

## 三、专业术语

### 1. GUI Agent

能够理解用户指令，并自动操作网页、手机或桌面应用程序的智能体。

### 2. GUI Grounding

根据自然语言指令，在界面截图中定位对应元素的能力。例如，根据“点击搜索按钮”，找到搜索按钮在屏幕上的坐标。

### 3. LVLM

Large Vision-Language Model，即大视觉语言模型，能够同时处理图片和文本，例如 Qwen-VL、GPT-4V 等。

### 4. ScreenSpot

论文提出的 GUI grounding 评测基准，包含移动端、桌面端和网页界面的截图及操作指令，用于测试模型能否准确找到目标元素。

### 5. Click Accuracy

点击准确率。如果模型预测的点击坐标落在正确界面元素的边界框内，则认为预测正确。

### 6. Bounding Box

目标元素在图片中的矩形区域，通常表示为左上角和右下角坐标。

## 四、核心方法

### 1. 从“读取结构化信息”转向“直接看截图”

传统 GUI Agent 往往依赖 HTML、DOM 或 Android UI hierarchy。SeeClick 不使用这些结构化信息，而是直接根据：

```text
当前界面截图 + 用户指令 + 历史动作
```

预测下一步操作，例如：

```text
click(0.49, 0.40)
```

这种方式理论上可以适用于网页、手机和桌面等不同平台。

### 2. 对视觉语言模型进行 GUI grounding 预训练

论文以 Qwen-VL 为基础进行继续训练，重点增强模型在 GUI 截图中的定位能力。训练任务主要包括：

- 根据文字指令预测界面元素的位置；
- 根据坐标识别对应元素的文字；
- 对移动端界面进行描述；
- 进行网页 OCR 和 UI 理解。

### 3. 自动构造网页训练数据

作者从 Common Crawl 中收集大量网页，通过解析网页 HTML 自动提取：

- 可见文字元素；
- 带有 `title` 属性的图标或交互元素。

这样可以自动建立“指令—目标元素—坐标”的训练样本，减少人工标注成本。

### 4. 结合移动端和通用视觉语言数据

除了网页数据，作者还加入移动 UI 数据和通用视觉语言数据：

- 移动端控件描述；
- 移动端 UI grounding；
- UI 总结；
- 通用图像描述和视觉推理。

加入通用数据的目的是避免模型在学习 GUI 操作后丧失原有的视觉理解和推理能力。

## 五、实验结果

### 1. ScreenSpot 上的 GUI 定位结果

ScreenSpot 的平均准确率如下：

| 模型 | 平均准确率 |
|---|---:|
| MiniGPT-v2 | 5.7% |
| Qwen-VL | 5.2% |
| GPT-4V | 16.2% |
| Fuyu | 19.5% |
| CogAgent | 47.4% |
| **SeeClick** | **53.4%** |

SeeClick 超过了 CogAgent，说明专门的 GUI grounding 训练能够显著提升模型定位界面元素的能力。

### 2. MiniWob 网页任务

在只使用图片的设置下，SeeClick 得分为 **67.0%**，高于使用相同规模训练数据的 Qwen-VL（48.4%），也超过了视觉方法 Pix2Act（64.6%）。

### 3. AITW 手机任务

在 Android 自动化任务 AITW 上，SeeClick 平均得分为 **59.3%**，点击准确率为 **66.4%**，均优于 Qwen-VL。

### 4. Mind2Web 真实网页任务

在 Mind2Web 上，SeeClick 的步骤成功率明显高于 Qwen-VL：

| 测试设置 | Qwen-VL | SeeClick |
|---|---:|---:|
| Cross-Task | 13.3% | **25.5%** |
| Cross-Website | 9.2% | **16.4%** |
| Cross-Domain | 12.0% | **20.8%** |

### 5. 实验总体结论

论文在多个任务上观察到一致趋势：

```text
GUI grounding 能力提升
        ↓
点击位置更加准确
        ↓
单步操作成功率提高
        ↓
多步 GUI 任务完成效果变好
```

## 六、优点

### 1. 思路简单且具有通用性

只依赖截图的方案不需要针对每个平台获取不同格式的结构化信息，因此更容易跨网页、手机和桌面平台迁移。

### 2. 抓住了视觉 GUI Agent 的核心瓶颈

论文没有只关注语言推理或任务规划，而是指出“能否准确找到目标控件”是实际交互中的基础问题。

### 3. 训练数据构造方法具有可扩展性

利用网页 HTML 自动提取元素，可以快速构造大规模 grounding 数据，降低人工标注成本。

### 4. 评测较为全面

论文同时评估了：

- GUI 元素定位；
- 简单网页操作；
- Android 手机操作；
- 真实网站操作。

这使得实验不仅能说明模型“看得准”，也能说明它“用得起来”。

## 七、缺点与局限

### 1. 动作空间仍然不完整

模型主要支持点击、输入、选择和滑动，对拖拽、双击、长按等复杂操作支持不足。

### 2. 仍然需要针对任务进行训练

SeeClick 并不是完全零样本的通用 Agent。要完成具体的多步任务，仍然需要使用对应任务的数据进行训练。

### 3. 复杂网页上仍然不如 HTML Agent

结构化 Agent 可以从候选元素中进行选择，而视觉 Agent 需要直接预测像素坐标，因此在复杂网页上更容易出错。

### 4. 图标和控件定位比较困难

相比文字，图标、按钮和其他 widget 通常更加难以识别和定位。

### 5. 不同平台混合训练可能产生干扰

网页、手机和桌面界面的视觉分布差异较大，将所有任务统一训练后，性能反而出现轻微下降。

## 八、可借鉴的思路

### 1. 把复杂问题拆成基础能力

GUI Agent 看似是一个完整的自动化任务，但可以拆成：

1. 界面理解；
2. 目标定位；
3. 动作预测；
4. 多步规划。

其中，目标定位是后续动作执行的基础。类似地，在其他智能体项目中，也可以先识别最底层、最容易成为瓶颈的能力，再针对性优化。

### 2. 充分利用已有结构化信息自动生成数据

论文通过 HTML 自动提取网页元素，构造了大量训练样本。这个思路可以迁移到其他项目中：

- 从网页日志生成操作数据；
- 从应用程序事件记录生成动作标签；
- 从已有标注反向构造问答任务；
- 用规则自动生成多种指令表达。

### 3. 同时训练“正向定位”和“反向识别”

SeeClick 不仅训练：

```text
指令 → 坐标
```

还训练：

```text
坐标 → 元素描述
```

这有助于模型建立“语言—位置—界面元素”之间的双向联系。对于多模态项目，可以考虑设计相互对应的正向和逆向任务。

### 4. 建立专门的中间能力评测集

ScreenSpot 并不直接测试完整任务，而是单独测试 GUI grounding。这种设计值得借鉴：对于复杂系统，除了看最终任务成功率，还应该建立能够衡量关键中间能力的 benchmark。

### 5. 不只比较最终分数，还要研究能力之间的关系

论文分析了 grounding 分数与下游任务性能之间的相关性。做项目时，也可以记录：

- 感知能力指标；
- 定位能力指标；
- 规划能力指标；
- 最终任务成功率。

这样更容易判断性能提升究竟来自哪里。

## 九、个人总结

SeeClick 最值得关注的地方，是它把 GUI Agent 的问题从“让大模型完成电脑操作”进一步细化为“让模型准确理解并定位界面元素”。论文说明，视觉 Agent 不仅需要具备语言推理能力，还必须能够把语言指令映射到屏幕中的具体位置。这个思路对网页自动化、手机助手、桌面 Agent 以及多模态交互系统都有较强的参考价值。

不过，SeeClick 仍处于视觉 GUI Agent 的早期阶段。它在复杂操作、长期规划、跨平台统一训练和安全控制方面仍有较大提升空间。

