# 《The Agentic Researcher: A Practical Guide to AI-Assisted Research in Mathematics and Machine Learning》读后感

> 本文是一份基于论文原文的阅读笔记与个人理解，重点关注：论文提出了什么、如何实现、证据是否充分，以及对使用 Codex/Claude 等 AI 编程代理开展科研的可借鉴之处。

---

## 一、基本信息

| 项目 | 内容 |
|---|---|
| 论文标题 | **The Agentic Researcher: A Practical Guide to AI-Assisted Research in Mathematics and Machine Learning** |
| 中文理解 | **智能体研究者：数学与机器学习中 AI 辅助研究的实践指南** |
| 作者 | Max Zimmer、Nico Pelleriti、Christophe Roux、Sebastian Pokutta |
| 机构 | 德国柏林数学与科学研究相关机构，包括 Zuse Institute Berlin、Technische Universität Berlin |
| 论文类型 | 方法框架与案例研究型论文 |
| 研究领域 | AI-assisted research、Agentic AI、机器学习、数学研究、科学计算 |
| 版本 | arXiv:2603.15914v1 |
| 时间 | 2026 年 3 月 16 日 |
| 项目地址 | [The-Agentic-Researcher](https://github.com/ZIB-IOL/The-Agentic-Researcher) |

---

## 二、一句话介绍

这篇论文提出了一套让通用 CLI 编程代理成为“科研助理”的工作流：研究者负责提出问题、确定方向和最终审查，AI 负责在明确规则约束下进行代码实现、实验搜索、数学推导、验证、记录和迭代。

换句话说，论文的核心不是训练一个新的科研模型，而是回答一个实践问题：

> **如何把 Codex、Claude Code、Gemini CLI、OpenCode 等通用 AI 编程代理，放进一个可靠、可复现、可审计的科研流程中？**

---

## 三、论文试图解决的问题

目前关于 AI 科研的讨论，很多集中在“AI 能否自动发现定理、提出算法或完成实验”。作者认为，另一个同样重要但容易被忽略的问题是：

> **研究人员应该如何在日常科研中使用这些 AI 系统？**

真实科研通常不是一次性执行一个固定目标，而是一个不断调整的过程。研究者需要持续判断：

- 哪个问题值得研究；
- 哪个假设值得验证；
- 实验结果是否可信；
- 失败是由于想法错误还是代码 bug；
- 是否需要改变研究方向；
- 一个结果是否具有原创性；
- 哪些结果足以写进论文。

因此，作者不赞成把研究者完全移出流程，而是提出一种“人机共创”模式：

- **人类研究者**：提出问题、制定方向、提供领域知识、审查结果、判断科学意义；
- **AI 代理**：执行代码、跑实验、尝试变体、调试、做数值验证、整理报告和提出下一步实验。

---

## 四、AI 参与科研的五个层级

论文提出了五级 AI integration taxonomy，用来描述 AI 深度参与科研的程度。

### Level 0：Classical，传统研究

完全不使用 AI，依靠传统工具开展研究，例如：

- LaTeX；
- Mathematica、MATLAB；
- Python、Julia、PyTorch；
- 人工查阅文献、设计实验和分析结果。

作者并不认为这一层级已经过时，它仍然是合理且普遍的研究方式。

### Level 1：AI as Consultant，AI 作为顾问

研究者使用 ChatGPT、Claude、Gemini 等聊天模型进行：

- 概念解释；
- 文献初步检索；
- 头脑风暴；
- 证明思路检查；
- 调试建议。

此时，研究者仍然完成主要的智力工作，AI 主要提供局部帮助。

### Level 2：AI as Typist，AI 作为打字员

AI 负责生成代码或文字，例如：

- 自动补全；
- 生成函数；
- 编写 LaTeX 段落；
- 根据自然语言描述生成程序。

但是，AI 通常不会自己执行和迭代，研究者需要手动运行、检查和修改。

### Level 3：AI as Collaborator，AI 作为协作者

研究者使用 CLI coding agent。代理能够：

- 读取代码库；
- 修改文件；
- 执行 shell 命令；
- 运行测试和实验；
- 根据结果继续调试。

研究者描述任务，AI 负责“如何实现”。但每完成一个任务后，AI 通常仍然等待研究者安排下一步。

### Level 4：AI as Research Associate，AI 作为研究助理

这是论文重点讨论的层级。研究者提供：

- 研究问题；
- 初步假设；
- 评价指标；
- 代码、数据和工具；
- 计算资源；
- 先前工作和已有结果。

然后，AI 自主执行较长时间的研究循环：

1. 理解问题；
2. 制定计划；
3. 实现方法；
4. 运行实验；
5. 分析结果；
6. 记录成功和失败；
7. 更新报告与 TODO；
8. 设计下一步实验。

研究者并不是完全不参与，而是从“每一步亲自操作”转变为：

- 方向设置；
- 阶段性审查；
- 结果验证；
- 必要时纠偏。

---

## 五、核心方法：Agentic Research Framework

论文的核心框架并不是重新训练一个 AI 模型，而是在通用 CLI 代理上叠加一套研究工作制度。

### 5.1 研究项目启动时需要提供三类输入

#### 1. 研究问题

包括：

- 问题定义；
- 研究目标；
- 初步假设；
- 评价指标；
- 成功标准。

#### 2. 工具、方法和数据

包括：

- 现有代码库；
- Python、Julia、PyTorch 等工具；
- 数据集；
- GPU 或集群资源；
- 自定义脚本；
- 运行时间和内存限制。

#### 3. 先验知识

包括：

- 相关论文；
- LaTeX 推导；
- 之前尝试过的方法；
- 初步实验结果；
- 已知的失败方向。

作者强调，给代理的上下文越完整，长期自主研究的效果越好。

### 5.2 持久化指令文件

框架使用类似 `INSTRUCTIONS.md` 的持久化指令文件。它包含：

- 通用科研规范；
- 当前项目的具体要求；
- 评价指标和约束；
- 研究者提供的领域知识。

代理在每次新会话或者上下文恢复时重新读取这些信息，从而避免只依赖单次对话的短期记忆。

### 5.3 两个关键研究文档

#### `report.tex`

记录所有实验、推导、结果和分析。每个实验需要包含：

- **Goal**：目标；
- **Hypothesis**：假设；
- **Method**：数学方法或算法方法；
- **Implementation**：修改了哪些代码；
- **Results**：实验结果和指标；
- **Analysis**：结果说明；
- **Next Steps**：下一步计划。

#### `TODO.md`

记录：

- 未完成的任务；
- 未解决的问题；
- 未验证的结论；
- 延后处理的实验；
- 需要研究者做决定的事项。

这两个文件相当于 AI 的外部研究记忆。

### 5.4 八步实验循环

每个实验遵循：

```text
Explore → Plan → Implement → Evaluate → Analyze → Record → Commit → Iterate
```

具体来说：

1. **Explore**：阅读代码、论文和已有实验；
2. **Plan**：确定这次只研究什么；
3. **Implement**：修改代码；
4. **Evaluate**：运行评估；
5. **Analyze**：分析结果和失败原因；
6. **Record**：写入研究报告；
7. **Commit**：使用 Git 保存版本；
8. **Iterate**：根据结果设计下一轮。

### 5.5 Git 作为实验记录系统

论文没有额外设计复杂的实验管理平台，而是直接使用 Git：

- 每个实验对应一个 commit；
- 不同方向使用不同分支；
- 重要结果使用 tag；
- Git worktree 用于并行实验。

实验提交信息可以类似：

```text
exp(E001): test normalized optimizer -- perplexity=33.35
```

这样可以追踪每次实验改了什么、得到什么结果以及结果之间的关系。

---

## 六、十条核心“科研戒律”

论文的十条戒律是框架的核心。作者没有只告诉 AI“要严谨”，而是把科研规范写成可执行、可检查的具体规则。

### 1. Never Break a Promise：不要违背承诺

如果 AI 说要运行实验、修改代码或检查某个问题，就应该完成。无法完成时必须说明原因并记录下来。

**解决的问题：** AI 先说“我接下来会做某事”，然后中途跳到其他任务。

### 2. Never Manipulate Evaluation：不要操纵评估

不能为了得到更好的结果而偷偷改变：

- 指标；
- 测试集；
- 固定超参数；
- 问题定义；
- 评估样本数量；
- 随机种子。

**解决的问题：** AI 可能“善意地”修改评估条件，从而产生不公平的优势。

### 3. Never Fabricate Citations：不能编造引用

每条文献都要核查：

- 标题；
- 完整作者；
- 年份；
- 期刊或会议；
- DOI、URL 或其他标识符。

如果找不到原始来源，就不能凭记忆补写。

### 4. Complete All Autonomous Work Before Reporting：先完成所有无需人类输入的工作

只要任务不需要研究者做决定，AI 就应该继续完成，而不是频繁停下来询问“是否继续”。

**解决的问题：** 长任务中 AI 过早汇报，导致大量可执行工作没有完成。

### 5. Make It Work Before Moving On：先修好，再判断想法是否失败

程序崩溃首先是实现问题，不应直接被认为是研究想法错误。

例如出现 OOM 时，应该检查：

- 是否创建了不必要的大矩阵；
- 是否需要清理 GPU 缓存；
- 是否可以分块计算；
- 是否可以使用 gradient checkpointing；
- 是否存在不合理的张量复制。

### 6. One Variable per Experiment：每个实验只改变一个变量

如果一次同时修改学习率、正则项和模型结构，即使性能提升，也无法知道究竟是什么因素导致提升。

这条规则使得实验结果具有可解释性。

### 7. Evaluate in Tiers：分层评估

论文提出三级评估：

- **Tier 1：秒级检查**，确认程序能否运行；
- **Tier 2：分钟级检查**，在小数据上判断是否存在信号；
- **Tier 3：完整评估**，在正式数据和完整指标上得出结论。

小规模实验只能用于调试，不能直接作为最终科学结论。

### 8. Bound Your Expectations：估计理论上限

在宣称一个方法有效之前，先估计理论上最多可能提升多少。

例如：

- 这个指标最多能改善多少？
- 当前结果距离 oracle 还有多远？
- 这个改进是否已经接近理论上限？

这样可以避免把很小的改进夸大为重大突破。

### 9. Record Everything：记录所有事情

成功和失败都必须写进报告。

每个实验都至少要记录：

- 目标；
- 假设；
- 方法；
- 实现；
- 结果；
- 分析；
- 下一步。

论文特别强调：

> 如果一个实验没有被记录，就相当于它没有发生过。

### 10. Verify Before Claiming：验证后再下结论

AI 应该假设自己可能是错的，并主动尝试推翻自己的结论：

- 写验证脚本；
- 测试边界情况；
- 随机化输入；
- 搜索反例；
- 与小规模暴力结果比较；
- 检查公式与代码是否一致。

同时将结论标记为：

- **Verified**：已验证；
- **Partially verified**：部分验证；
- **Unverified**：尚未验证。

---

## 七、领域专用规则

十条通用戒律之外，论文还给出了两类领域规则。

### 7.1 计算密集型研究

对于 GPU、深度学习和大规模数值实验：

- 每个独立实验尽量占用一块 GPU；
- 开始任务前检查 GPU 状态；
- 将长日志重定向到文件；
- 用轻量命令监控进度；
- OOM 时先系统排查内存；
- 多节点环境中只分发互相独立的任务。

### 7.2 数学研究

对于证明、推导和理论研究：

- 先写清楚推导，再实现代码；
- 明确定义所有符号、维度和索引；
- 先搜索反例，再正式尝试证明；
- 查阅已有工作，避免重复发现；
- 对负面结果进行同样严格的记录和分析。

---

## 八、案例研究与实验结果

论文共有六个案例，前三个面向机器学习，后三个面向数学与优化。

### 案例一：LLM 预训练中的优化器探索

#### 研究问题

Muon 优化器相较 AdamW 能够节省部分内存。问题是：

> 能否利用剩余的内存预算进一步提升 Muon 的预训练效果？

#### AI 做了什么

AI 在保持其他条件不变的情况下，逐一测试：

- 动量归一化；
- 正交化前处理；
- weight decay；
- 不同超参数；
- 与其他 Muon 变体的比较。

#### 结果

- 进行了 40 多个实验；
- 最佳配置相对 Muon 基线约有 5% 的验证困惑度改进；
- 相对 AdamW 约有 8% 的改进；
- 归一化和 weight decay 的效果大致可以叠加；
- 找到一个几乎不增加额外内存的版本；
- 多 GPU 并行运行；
- 整个自主实验会话持续超过 20 小时。

#### 说明

这个案例表明，AI 很适合在复杂的优化器设计空间中进行大量受控实验。但作者也指出，结果只在有限的架构、数据集和训练条件下验证，不能据此直接宣称方法具有普遍性或原创性。

### 案例二：LLM 剪枝中的权重重构

#### 研究问题

原有剪枝方法表现不稳定，有时甚至灾难性失败。任务原本是修复剪枝 mask。

#### AI 的关键发现

AI 分析失败原因时发现，剪枝后不同矩阵行的激活加权输出损失非常不平衡：

- 某些行损失超过 50%；
- 某些行损失不到 10%。

于是 AI 没有继续修补原方法，而是提出了一个简单的后处理权重重构方法，用来重新平衡剪枝后的激活。

#### 结果

- 在 125M 到 9B 参数的多个模型规模上有效；
- 覆盖 OPT、Qwen、Gemma 等架构；
- 在不同剪枝方法上都能改善困惑度；
- 困惑度改善约为 18% 到 50%；
- 只需要约 10 行代码；
- 额外计算开销低于 1%；
- 达到了最小二乘 oracle 改进的大约 92%。

#### 说明

这个案例体现了科研中的“意外发现”：AI 原本被要求修复一个失败方法，但在分析失败原因时找到了一个更有效的新方向。

### 案例三：LLM 量化中的列排序

#### 研究问题

GPTQ 量化逐列处理权重矩阵，处理顺序会影响误差传播。AI 研究了：

- 是否存在比 act-order 更好的排序；
- 排序效果是否依赖模型架构；
- 哪些条件下列排序最重要。

#### AI 做了什么

- 先分析误差传播机制；
- 实现七种排序策略；
- 先在单个矩阵上测试；
- 再在完整模型上评估；
- 编写公式验证脚本；
- 在 Qwen、Llama、Gemma、Mistral、Yi 五类模型上比较。

#### 结果

列排序的收益高度依赖架构：

- Llama-3.1-8B 上，困惑度改善约 74%；
- Gemma-2-9B 上，改善只有约 0.1%；
- Qwen-1.5B 上，改善约 20%；
- 没有一种排序方法在所有架构和 bit-width 上始终最好；
- 24 个实验中有 9 个为负结果。

AI 还发现了一个 group quantization 的实现 bug。修复前困惑度约为 437，修复后降到约 9.22。

#### 说明

这个案例说明，只在一个模型上实验很容易产生误导性结论。跨架构验证对于机器学习研究非常重要。

### 案例四：均匀凸集合上的 Frank-Wolfe 下界

#### 研究问题

对于 p-均匀凸集合，已有收敛上界，但此前缺少匹配的下界。目标是证明：

$\[
\Omega(1/T^{p/(p-1)})
\]$

#### AI 的研究过程

AI 先尝试推广已有的高维构造，但没有成功。随后转向直接分析 Frank-Wolfe 在特定最坏情形上的迭代轨迹：

1. 数值模拟迭代过程；
2. 观察迭代点的符号交替和低维轨迹；
3. 用数值实验估计关键常数；
4. 将数值规律转化为闭式表达；
5. 构造严格证明；
6. 使用 Julia 和高精度浮点数验证推导。

#### 结果

- 对所有 $\(p \geq 3\)$，证明了匹配的下界；
- 该下界与已知上界一致；
- 超过 30 个验证检查支持证明中的关键步骤；
- $\(2<p<3\)$ 的情况仍然开放。

#### 说明

这是“计算猜想—理论证明”的典型流程。数值实验不是证明，但可以帮助发现正确的证明结构。

### 案例五：混合整数优化中的多变量对偶收紧

#### 研究问题

Boscia 求解器已有针对单变量的 dual tightening。论文研究能否推广到多个变量，从而形成冲突约束或冲突图。

#### AI 的研究过程

AI：

- 从已有单变量定理出发；
- 推导多变量版本；
- 推出二元变量的 pairwise conflict；
- 推出更高阶冲突；
- 用符号工具进行大量检查；
- 对小规模实例进行穷举验证；
- 实现 ConflictGraph 数据结构；
- 通过 callback 集成到 Boscia。

验证过程中发现一个重要错误：初始推导中某个“至少”集合约束的界限方向写反了。如果不修正，会导致过于激进的变量固定。

#### 结果

- 测试了 33 个实例；
- 某类分区约束问题最多减少约 18.9% 的搜索节点；
- 很多实例没有节点减少；
- 所有实例都得到相同的最优目标值；
- 可分离二次问题没有收益，符合理论预期；
- 某个稀疏回归实例出现约 26 倍开销，暴露了实现瓶颈。

#### 说明

这个案例同时包含理论推导、验证、工程实现和性能测试，展示了 AI 如何参与完整研究链条。

### 案例六：K7 电网中的最大实数解数量

#### 研究问题

对于固定的 K7 电网结构，不同参数可能产生不同数量的实数潮流解。目标是搜索拥有更多实数解的参数配置。

#### AI 的研究过程

- 重建已有论文中的计算流程；
- 复现已有的解数量分布；
- 使用爬山、模拟退火和 warm start；
- 将随机采样改造成定向极值搜索；
- 对高解数量配置附近的参数进行扰动。

#### 结果

- 原有随机采样在 140 万个参数向量中最多找到 120 个非平凡解；
- 定向搜索找到 192 个非平凡解；
- 192 解的配置位于参数空间的一个小区域，而非孤立点；
- K7 的最大实数解数量仍然没有被严格确定。

#### 说明

这个案例说明，AI 也可以先复现已有工作，再将原有算法改造成探索极端情况的工具。

---

## 九、论文的主要优点

### 1. 讨论的是实际工作流，而不只是展示 AI 结果

很多 AI 科研论文关注“模型解决了什么问题”，而本文更关注：

- 研究者如何布置任务；
- AI 如何持续工作；
- 实验如何被记录；
- 结论如何被验证；
- 如何处理失败。

这使论文对普通研究者更有操作参考价值。

### 2. 把抽象的“严谨”转化成具体规则

例如：

- 每个实验只改一个变量；
- 先做小规模测试，再做完整测试；
- 失败也要记录；
- 不能修改评估标准；
- 下结论之前必须写验证脚本。

这些规则可以直接放进 `INSTRUCTIONS.md`，用于 Codex、Claude Code 等代理。

### 3. 强调失败实验和负结果

论文没有只展示成功案例，也讨论：

- Muon 相关方法的有限适用范围；
- 量化排序策略的失败；
- 数学构造推广的不成功；
- 冲突图在部分实例上没有收益；
- 某些实现带来的额外开销。

这比只展示“AI 发现了什么”更加可信。

### 4. 案例覆盖面较广

六个案例包括：

- 优化器搜索；
- 剪枝；
- 量化；
- 凸优化理论；
- 混合整数优化；
- 电网多项式方程。

因此，作者试图说明该框架不只适用于某一种模型或某一种实验。

### 5. 重视可复现性和可审计性

通过：

- Git；
- 报告文件；
- TODO；
- 验证脚本；
- 沙盒环境；
- 分阶段评估；
- 明确的实验提交信息；

使得研究过程能够被回看，而不是只剩下一个最终结论。

### 6. 明确保留人类责任

作者没有把 AI 描述成能够独立完成科研的神奇系统，而是强调：

- AI 可以扩大研究者的执行能力；
- 研究者仍然负责研究方向；
- 研究者仍然负责最终验证；
- AI 的结果不能自动等同于发表级成果。

---

## 十、论文的主要缺点和局限

### 1. 证据主要来自案例，而不是系统对照实验

论文展示了多个案例，但没有充分比较：

- 不使用框架时的科研效率；
- 使用普通 AI 对话时的表现；
- 使用不同规则时的差异；
- 不同模型或不同代理之间的差异。

因此，很难精确判断每一条戒律到底贡献了多少改进。

### 2. “十条戒律”的作用没有被逐条消融验证

论文认为十条戒律很重要，但没有系统回答：

- 去掉“每次只改一个变量”会损失多少；
- 去掉“记录一切”会导致什么程度的问题；
- 去掉验证规则后错误率会上升多少；
- 哪些戒律最重要，哪些可以合并。

从科学评价角度看，这些规则本身也需要 ablation study。

### 3. AI 的自主性仍然依赖高质量的人类输入

如果研究者没有提供：

- 清晰的问题；
- 合理的评价指标；
- 可靠的 baseline；
- 领域知识；
- 正确的代码和数据；

AI 可能会在错误方向上高效地运行大量实验。

所以，这个框架增强的是“研究执行能力”，不能替代研究问题选择和研究判断。

### 4. 原创性仍然无法由 AI 保证

即使 AI 检索了相关论文，也可能：

- 漏掉最新工作；
- 没有识别等价方法；
- 误判自己的组合是否新颖；
- 忽略并行发表的结果。

最终的相关工作和原创性判断仍需要领域专家完成。

### 5. 长期记忆问题仍未彻底解决

报告和 Git 能够提供外部记忆，但 AI 仍可能：

- 忘记细节；
- 重复失败实验；
- 误读旧结果；
- 在上下文压缩后丢失研究逻辑。

论文承认，复杂长期研究中的上下文管理仍是开放问题。

### 6. 计算资源和成本可能成为瓶颈

长时间自主运行需要：

- GPU；
- 存储空间；
- API 调用费用；
- 日志管理；
- 实验调度能力。

虽然模型在等待训练完成时不一定消耗很多 token，但完整项目仍可能产生较高成本。

### 7. 案例中的部分结论还没有达到发表级别

作者自己也强调，案例报告更多是“有意义的研究进展”，而不是已经完成、经过充分同行评审的独立论文。

例如：

- 某些机器学习结果只在有限模型和数据集上测试；
- K7 电网案例找到的是更强的计算下界，而不是严格最大值；
- Frank-Wolfe 结果只解决了 \(p\geq3\) 的范围；
- 某些方法还需要与同期工作进行更完整比较。

---

## 十一、可借鉴的思路

### 1. 把 AI 当作“实验执行者”，而不是答案生成器

不要只问：

> “请告诉我这个问题的答案。”

更适合的方式是：

> “请建立一个可以不断提出假设、运行实验、验证结果并记录历史的研究循环。”

### 2. 把研究规范写成可执行的规则

“要严谨”“要认真”通常太抽象。应该改写成：

- 每个实验只改一个变量；
- 每次结论必须有验证脚本；
- 未验证内容必须标记；
- 失败实验也要记录；
- 不能修改 baseline；
- 每次实验必须提交 Git。

### 3. 让报告文件成为 AI 的外部记忆

推荐至少维护：

```text
INSTRUCTIONS.md
RESEARCH_PLAN.md
TODO.md
report.md
```

其中：

- `INSTRUCTIONS.md`：规则；
- `RESEARCH_PLAN.md`：研究目标和边界；
- `TODO.md`：当前任务；
- `report.md`：完整实验历史。

### 4. 先建立可靠 baseline，再开始创新

任何新方法都应该先回答：

- baseline 是否能复现；
- 指标计算是否正确；
- 数据划分是否正确；
- 随机性是否受控；
- 结果是否与已有论文大致一致。

如果 baseline 不可靠，后面的改进也没有意义。

### 5. 使用“先便宜、后昂贵”的实验顺序

推荐：

```text
代码检查 → 小数据测试 → 小规模实验 → 完整实验 → 多次重复 → 跨模型验证
```

不要一开始就运行最昂贵的实验，也不要因为小实验失败就直接放弃一个想法。

### 6. 把失败变成信息

实验失败时，不要只记录“失败”。应该记录：

- 失败出现在哪里；
- 是理论问题还是实现问题；
- 是否存在 bug；
- 失败是否支持或反驳原假设；
- 是否值得继续修复。

### 7. 对机器学习方法进行跨数据集和跨架构验证

单一模型上的提升可能只是偶然现象。至少应考虑：

- 多个模型规模；
- 多个模型架构；
- 多个随机种子；
- 多个数据集或任务；
- 与多个强 baseline 比较。

### 8. 对数学问题采用“反例优先”策略

在花大量时间构造证明前，可以先：

- 随机生成小规模实例；
- 检查边界情况；
- 穷举小规模空间；
- 搜索反例；
- 用高精度数值验证公式。

如果反例存在，可以快速终止错误方向；如果没有反例，实验结果也可能揭示证明结构。

### 9. 把代码、实验和论文写作连接起来

不要把代码、结果和论文笔记分成完全孤立的部分。每个实验最好同时更新：

- 实验代码；
- 结果表；
- 图形；
- 分析文字；
- TODO；
- Git 历史。

这样可以减少最后写论文时重新整理材料的工作。

### 10. 让人类在关键节点介入，而不是每一步都介入

比较合理的分工是：

- AI 自主执行重复实验和调试；
- 人类检查研究方向和关键结果；
- AI 根据反馈继续探索；
- 人类进行最终验证和写作判断。

这比两种极端都更好：

- 让人类亲自操作每一步，效率低；
- 完全放任 AI，可靠性不足。

---

## 十二、我对这篇论文的总体评价

我认为，这篇论文最有价值的地方不是证明了“AI 可以替代研究者”，而是给出了一个更现实的判断：

> **AI 的科研价值不只取决于模型有多聪明，也取决于研究流程是否被设计得足够清晰、可验证和可追踪。**

如果只把 Codex 当成一个聊天工具，它主要只能帮助写代码、解释概念和生成文本；但如果给它配上：

- 持久化规则；
- 明确的研究计划；
- 实验记录；
- TODO 管理；
- Git 版本控制；
- 自动验证脚本；
- 分阶段评估；
- 人类阶段性审查；

那么它就可能承担更大范围的研究执行工作。

不过，这种方法的本质仍然是“人类主导的人机协作”，而不是完全自主的科学发现。AI 可以更快地搜索、实现、比较和记录，但它仍然可能：

- 误解问题；
- 编造解释；
- 忽视重要文献；
- 误判结果；
- 在错误方向上高效工作。

因此，最值得借鉴的不是简单地把十条戒律复制到提示词里，而是建立完整的科研制度：

```text
明确问题
→ 固定研究规则
→ 建立 baseline
→ 单变量实验
→ 分层评估
→ 主动验证
→ 记录失败
→ Git 留痕
→ 人类审查
→ 再进入下一轮
```

这也是论文标题中“Agentic Researcher”最重要的含义：AI 不是独立替代研究者，而是在人类设定方向和承担责任的前提下，成为一个可以长时间工作的研究助理。

---
