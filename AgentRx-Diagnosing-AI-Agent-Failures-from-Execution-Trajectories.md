# AgentRx：基于执行轨迹诊断 AI Agent 失败原因

## 1. 基本信息

- **论文标题**：AgentRx: Diagnosing AI Agent Failures from Execution Trajectories
- **作者**：Shraddha Barke, Arnav Goyal, Alind Khare, Avaljot Singh, Suman Nath, Chetan Bansal
- **研究机构**：Microsoft Research、Microsoft、University of Illinois Urbana-Champaign
- **论文类型**：AI Agent 可靠性与失败诊断研究
- **研究主题**：AI Agent 失败定位、根因分析、LLM-as-a-Judge、执行轨迹分析
- **代码与数据**：论文提供了 AgentRx 数据集和开源代码

## 2. 一句话介绍

这篇论文提出了 AgentRx，通过分析 AI Agent 的完整执行轨迹，定位导致任务最终失败的“第一次不可恢复错误”，并判断其具体失败类型。

## 3. 专业术语

| 术语 | 含义 |
|---|---|
| **AI Agent** | 能够自主规划、调用工具并完成多步任务的智能系统 |
| **Execution Trajectory** | Agent 从开始到结束的完整执行过程，包括对话、工具调用和工具返回结果 |
| **Critical Failure** | 第一次导致任务无法继续恢复的关键错误 |
| **Root Cause** | 导致最终失败的根本原因，而不是后续出现的表面症状 |
| **Failure Attribution** | 判断失败发生在哪一步、由谁造成以及属于什么类别 |
| **Constraint** | 根据工具规则、领域政策和轨迹内容生成的检查条件 |
| **Validation Log** | 记录约束是否违反以及相关证据的验证日志 |
| **LLM-as-a-Judge** | 使用大语言模型对 Agent 行为进行评价或分类 |
| **Grounded Theory** | 从实际案例中归纳概念和分类，而不是预先规定分类体系 |

## 4. 核心方法

AgentRx 的基本流程如下：

1. **统一轨迹格式**  
   将不同 Agent 系统的日志转换为统一的中间表示，包含 Agent、工具、输入、输出和步骤信息。

2. **生成约束**  
   根据工具的输入输出规范、领域政策、用户任务以及当前轨迹生成检查规则。

3. **逐步验证**  
   在每个步骤检查 Agent 是否违反了这些约束，并保存违反约束的具体证据。

4. **根因判断**  
   将完整轨迹、验证日志和失败分类清单交给 LLM，判断哪个步骤是第一次不可恢复失败，以及它属于哪种失败类型。

论文总结出九类主要失败原因：

1. 指令或计划遵循失败
2. 发明或篡改信息
3. 无效工具调用
4. 误解工具输出
5. 用户意图与执行计划不匹配
6. 用户意图信息不完整
7. 用户要求超出系统能力
8. 触发安全策略或外部访问限制
9. 系统或网络故障

## 5. 实验结果

论文构建了一个包含 **170 条失败轨迹、覆盖 11 类任务场景**的数据集，涉及零售 API、云服务故障排查、网页与文件操作、软件开发和数学推理等任务。

主要结果如下：

- AgentRx 在多个数据集上整体优于已有的失败归因方法。
- 论文报告其失败步骤定位能力平均提升约 **75%**。
- 根因类别判断相比作者使用的基线提升约 **30%**。
- 在 Magentic-One 等长轨迹任务中，逐步生成约束比一次性生成约束更有效。
- 违反约束的证据和分类清单结合使用时，通常能够获得更稳定的诊断结果。
- 逐步分析更准确，但成本更高；一次性分析成本较低，更适合大规模监控。

论文还发现：最长的轨迹不一定是最早失败的轨迹。很多 Agent 会经历多个错误，其中一些错误可以被后续步骤修复，真正需要关注的是第一个无法恢复的错误。

## 6. 优点

### 6.1 明确区分“第一次错误”和“根因错误”

论文没有简单地把第一个错误当成失败原因，而是判断该错误是否被后续操作修复。这一设计更符合实际调试需求。

### 6.2 具有较好的跨领域能力

AgentRx 不依赖某一个具体应用，而是通过统一轨迹格式和约束生成机制，适配不同工具、不同任务和不同 Agent 系统。

### 6.3 诊断过程具有可解释性

系统不仅给出失败步骤和失败类别，还提供违反的约束及支持证据，便于人工复核。

### 6.4 结合程序检查与语义判断

对于参数格式、数量计算等问题，可以使用程序直接检查；对于计划是否合理、意图是否匹配等问题，则使用 LLM 进行语义判断，二者互相补充。

## 7. 缺点与局限

### 7.1 约束可能产生误报

错误的或不相关的约束可能误导判断模型，使它把后续症状误认为根因。

### 7.2 仍然依赖 LLM 的判断能力

AgentRx 不是完全形式化的验证系统，最终根因判断仍然可能受到模型幻觉、上下文长度和判断偏差的影响。

### 7.3 成本较高

逐步生成约束需要多次调用模型，在长轨迹或大规模系统中会带来额外成本。

### 7.4 失败分类并不一定完整

论文归纳出的九类失败来自已有数据集，面对新的 Agent 类型或新的应用环境时，可能需要增加新的类别。

### 7.5 数据隐私问题

执行轨迹可能包含用户信息、业务数据和敏感工具调用记录，因此实际部署时需要进行隐私保护。

## 8. 可借鉴的思路

### 8.1 不要只关注最终结果

在设计 Agent 评估系统时，除了判断任务是否成功，还应该记录：

- 从哪一步开始出现偏差；
- 哪些错误后来被修复；
- 哪一步开始无法恢复；
- 错误属于哪一类。

### 8.2 为系统行为设计可检查的约束

可以把自然语言规范转化成具体检查项，例如：

- 工具参数是否符合 schema；
- Agent 的结论是否与工具返回一致；
- 是否执行了必要的身份验证；
- 是否使用了不存在的文件、链接或数据；
- 是否在正确的步骤调用工具。

### 8.3 将程序验证和模型判断结合起来

能用程序检查的内容尽量程序化，涉及复杂语义的内容再交给 LLM 判断。这样可以降低完全依赖 LLM 带来的不确定性。

### 8.4 记录带证据的诊断日志

一个好的 Agent 监控系统不应只输出“失败了”，还应说明：

- 失败发生在哪一步；
- 违反了什么规则；
- 相关输入和输出是什么；
- 为什么认为它是根因。

### 8.5 关注可恢复性

在我的项目中，可以将每个错误标记为“可恢复”或“不可恢复”，并进一步分析 Agent 的恢复策略是否有效。这比单纯统计错误数量更有价值。

## 9. 个人理解

我认为这篇论文最重要的贡献，不只是提出了一个新的诊断框架，而是改变了 AI Agent 的评估视角：Agent 的可靠性不能只用最终成功率衡量，还应该分析它在执行过程中如何犯错、是否能够恢复，以及最终从哪里开始失去完成任务的可能性。

这种思路对构建更可靠的多 Agent 系统、自动化工作流和工具调用系统都有较强的参考价值。

## 10. 总结

AgentRx 将 AI Agent 的失败诊断转化为一个“轨迹分析 + 约束验证 + 根因分类”的问题。它能够帮助研究者和开发者回答三个关键问题：

1. Agent 在哪一步真正失败？
2. 失败属于什么类型？
3. 有什么证据支持这个判断？

虽然该方法仍然存在误报、成本和模型依赖等问题，但它为 AI Agent 的可解释评估和自动化调试提供了一个清晰、实用的框架。
